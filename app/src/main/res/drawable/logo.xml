<?xml version="1.0" encoding="utf-8"?>
<level-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--  Seems like StateListDrawable would've been right, but I didn't see
          a way to select a specific state.  Using this, we're going to call
          setLevel(...) based on the user's selection. -->
    <item android:drawable="@drawable/logo_default"
          android:minLevel="0"
          android:maxLevel="0"/>
    <item android:drawable="@drawable/logo_nobogo"
          android:minLevel="1"
          android:maxLevel="1"/>
    <item android:drawable="@drawable/logo_nogood"
          android:minLevel="2"
          android:maxLevel="2"/>
</level-list>